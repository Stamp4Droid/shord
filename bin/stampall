#!/bin/bash

#TODO: handle symlinks in a platform-independent way
STAMP_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}")"/.. && pwd )"
BASE_DIR="$( pwd )"

for f in `ls "$1"/*.apk`
do
	shift
	ant -f "$STAMP_DIR/ant/stamp.xml" -Dbasedir="$BASE_DIR" analyze -Dapp="$f" "$@"
done